# Drop 3 image files here — they become the app's built-in default

Save your images with these EXACT names and the app uses them for everyone
(no per-user upload needed). Names/extensions must match exactly.

| Filename (here in public/) | Your image | Where it shows |
|---|---|---|
| `reaper.png`        | circular wolf **bust** (transparent cutout) | avatar in the Death dock + countdown |
| `hero-wolf.png`     | **full-body** wolf (transparent cutout)     | the big figure in the dashboard hero |
| `bg-graveyard.jpg`  | moonlit **graveyard** (no wolf)             | background behind every screen |

Notes
- Use **PNG** for the two wolves (transparent cut-out → no box around the wolf).
- Use **JPG** for the background.
- Until a file exists, the app falls back to the CSS moon/clouds + SVG wolf, so
  nothing ever breaks.
- An in-app uploader (Settings → Appearance) can still override these per device.
